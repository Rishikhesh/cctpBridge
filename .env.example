# Reown / WalletConnect project ID — get a free one at https://cloud.reown.com
# Used by both Stellar and EVM WalletConnect (one at a time per page —
# WalletConnect v2's Core is a page singleton, so the UI gates dual WC).
VITE_WALLETCONNECT_PROJECT_ID=
